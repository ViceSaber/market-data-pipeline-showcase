[project]
name = "market-data-pipeline-showcase"
version = "0.1.0"
description = "Market-data pipeline public showcase"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "apscheduler>=3.10.0",
]
